# Claude Task: Mount Metrics Router

**Project:** Agent Auth Proxy
**Scope:** Single focused improvement

**Task:**
Mount the metrics router from `app/metrics.py` so the `/metrics` Prometheus endpoint is actually reachable.

**Requirements:**
- Import and include the router in `app/main.py`
- Make minimal changes

Focus only on this.