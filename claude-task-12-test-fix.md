# Claude Task: Fix Health Check Test

**Project:** Agent Auth Proxy
**Scope:** Test fix

**Task:**
Fix the test that asserts `"healthy"` when the health endpoint actually returns `"ok"`.

**Requirements:**
- Update the test in `tests/` to match the actual response
- Make minimal changes

Focus only on this.