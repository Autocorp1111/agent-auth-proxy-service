# Claude Task: Remove Duplicate get_db

**Project:** Agent Auth Proxy
**Scope:** Cleanup

**Task:**
Remove the duplicate `get_db` definition from `app/dependencies.py` since it already exists in `app/database.py`.

**Requirements:**
- Delete the duplicate function
- Update any imports if needed
- Make minimal changes

Focus only on this cleanup.